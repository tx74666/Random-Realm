<Window x:Class="Product1.Experiment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Your Window" Height="500" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Button  Click="Button_Click">page1</Button>
            <Button  Click="Button_Click1">page2</Button>
            <Frame Name="framemain" NavigationUIVisibility="Hidden"  Grid.Row="1" Grid.Column="1"/>
        </StackPanel>
    </Grid>
</Window>


